using System;
using NUnit.Framework;
using easypost.Tests;

namespace easypost.Client.Tests.Unit
{
    // TODO: Implement or remove
    //[TestFixture]
    public class PostClientUnitTestFixture : BaseUnitTestFixture
    {
        // TODO: Implement or remove
        //[Test]
        public void Test_Post()
        {
            throw new NotImplementedException ();

            /*// Neither of these values matter because the HTTP requests are mocked
            var host = "localhost";
            var port = 8088;

            var client = new PostClient (host, port);
            client.HttpRequests = new MockHttpRequestTool ();

            var content = "Hello world";

            var fileName = "TestFile.txt";

            client.Post(content, fileName, folderName, securityKey);*/

        }
    }
}

